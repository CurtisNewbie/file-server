# base path
base.path=/home/zhuangyongj/file/fileserver

# change spring boot's root log level
logging.level.root=INFO

server.port=8443

# ------------------ redis ---------------------------------------------
redisson-config=redisson-test.yaml

# ------------------ SSL Certificate -----------------------------------
# The format used for the keystore. It could be set to JKS in case it is a JKS file
server.ssl.key-store-type=PKCS12
# The path to the keystore containing the certificate
server.ssl.key-store=classpath:cert/fileServer.p12
# The password used to generate the certificate
server.ssl.key-store-password=zhuangyongj
# The alias mapped to the certificate
server.ssl.key-alias=fileServer

# ------------------ distributed-task-module -----------------------------------
distributed-task-module.enabled=true

# -------------------auth-module --------------------------------------------
authmodule.application-name=file-service

# ----- nacos heartbeat thread count
namingClientBeatThreadCount=2

# ----- nacos naming polling thread count
namingPollingThreadCount=2
